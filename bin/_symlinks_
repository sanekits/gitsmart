# List of paths in .local/bin/gitsmart/ which get symlinked to
# .local/bin on setup
#
# TODO: list your scripts which need to be on the PATH, one per line
#
#   -  e.g.:
#   foo-script.sh #help Prints 'foo' on demand
#
#   -  Or you can run a shell command to output the help text.  Optional
#   - (${SHELLKIT_HELP_SYMBOL} expands to the script name to avoid repetition.)
#   my-script.sh #help@ ${SHELLKIT_HELP_SYMBOL} --help
#
gitsmart.sh
git-dirty # Exit 0 if any dir in arglist is dirty
git-make-child-clone.sh
git-remote-show-urls.sh
git-wc-inventory
git-wc-inventory-mark-done
gitsmart-help.sh
git-log-compact
git-reset-very-hard.sh  # Reset to ref=\$1 and delete untracked files
git-status-bulk.sh # Show git-status for a series of dirs, with de-dupe
git-wc-map.mk # Map all of the git working copies under current dir
